export enum VI {
    OUTLINED = 'outlined',
}
export type TypeInpVariant = 'outlined';

export enum PIL {
    LEFT = 'left',
    TOP = 'top',
    RIGHT = 'right',
}

export type TypePositionInpLabel = 'top' | 'left' | 'right';
